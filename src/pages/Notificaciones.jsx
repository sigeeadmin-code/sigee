import React from 'react';
import Placeholder from './Placeholder.jsx';
export default function Notificaciones(props) {
  return <Placeholder title={(props.title || 'Notificaciones') + ' (pendiente de pasar el archivo real)'} />;
}
